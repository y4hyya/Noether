# Noether Keeper Bot Configuration
# Copy this file to .env and fill in your values

# Keeper's Stellar secret key (for signing liquidation transactions)
# You can use the same key as ADMIN_SECRET_KEY or a separate one
KEEPER_SECRET_KEY=S...

# Network configuration (testnet or mainnet)
NETWORK=testnet

# RPC URL (leave default for testnet)
RPC_URL=https://soroban-testnet.stellar.org

# Network passphrase
NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Keeper settings
POLL_INTERVAL_MS=10000        # How often to check positions (10 seconds)
MIN_KEEPER_REWARD=1000000     # Minimum reward to attempt liquidation (0.1 USDC with 7 decimals)
MAX_GAS_PRICE=100             # Maximum gas price to pay

# Contract addresses will be loaded from contracts.json
# Or you can set them manually:
# NEXT_PUBLIC_MARKET_ID=C...
# NEXT_PUBLIC_ORACLE_ADAPTER_ID=C...
# NEXT_PUBLIC_VAULT_ID=C...
